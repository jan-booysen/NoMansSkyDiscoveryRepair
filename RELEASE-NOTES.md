# v1.0.0

Initial public release.

- Select a discovery owner by UID/name.
- Preserve `Available` and `Enqueued` discovery data.
- Preserve the rest of the exported JSON structure.
- Create a backup before processing.
- Write a separate repaired JSON file.
- Require explicit `REPAIR` confirmation.
